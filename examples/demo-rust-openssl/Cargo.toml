[package]
name = "demo-rust-openssl"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.12", default-features = false, features = ["blocking", "native-tls"] }

[profile.release]
strip = false
