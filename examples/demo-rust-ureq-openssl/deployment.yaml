apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-rust-ureq-openssl
  labels:
    app: demo-rust-ureq-openssl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-rust-ureq-openssl
  template:
    metadata:
      labels:
        app: demo-rust-ureq-openssl
      annotations:
        # Enable Kloak for this pod
        getkloak.io/enabled: "true"
    spec:
      containers:
        - name: demo-app
          image: kloak-demo-rust-ureq-openssl:latest
          imagePullPolicy: Never
          env:
            - name: SECRET_ALLOWED_FILE
              value: "/etc/secrets/allowed/api-key"
            - name: SECRET_BLOCKED_FILE
              value: "/etc/secrets/blocked/api-key"
            - name: TARGET_URL
              value: "https://httpbin.org/headers"
            - name: REQUEST_INTERVAL
              value: "5"
          volumeMounts:
            - name: secret-allowed-vol
              mountPath: "/etc/secrets/allowed"
              readOnly: true
            - name: secret-blocked-vol
              mountPath: "/etc/secrets/blocked"
              readOnly: true
      volumes:
        - name: secret-allowed-vol
          secret:
            secretName: secret-allowed
        - name: secret-blocked-vol
          secret:
            secretName: secret-blocked
